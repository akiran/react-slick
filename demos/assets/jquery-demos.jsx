var React = require('react');
var $ = require('jquery');

var JqueryDemos = React.createClass({
  render: function () {
    return (
      <div>jquery</div>
    );
  }
});

module.exports = JqueryDemos;